## Text Preprocessing

Applied preprocessing steps:
- Lowercasing (to handle uppercase/lowercase inconsistencies)
- Removing digits (as required in the task)
- Normalizing extra whitespaces

Note: Raw files are kept unchanged in `data/raw/`, and cleaned versions are saved under `data/processed/`.
